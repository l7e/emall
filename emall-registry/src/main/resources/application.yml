spring:
  application:
    name: emall-registry
server:
  port: 10086

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka
    fetch-registry: false         #不拉取服务列表
    register-with-eureka: false   #不注册自己